apiVersion: numaplane.numaproj.io/v1alpha1
kind: ISBServiceRollout
metadata:
  name: my-isbsvc
  namespace: example-namespace
spec:
  strategy:
    progressive:
      assessmentSchedule: "0,0,0,10"
  interStepBufferService:
   spec:
      jetstream:
        version: 2.10.3
        persistence:
          volumeSize: 1Gi
